{% extends 'template.html.twig' %}

{% block title %}Thepage{% endblock %}

{% block body %}
    <h1>Thepage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idthepage</th>
                <td>{{ thepage.idthepage }}</td>
            </tr>
            <tr>
                <th>Thetitle</th>
                <td>{{ thepage.thetitle }}</td>
            </tr>
            <tr>
                <th>Thelongtext</th>
                <td>{{ thepage.thelongtext }}</td>
            </tr>
            <tr>
                <th>Thetime</th>
                <td>{{ thepage.thetime ? thepage.thetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thepage_index') }}">back to list</a>

    <a href="{{ path('thepage_edit', {'idthepage': thepage.idthepage}) }}">edit</a>

    {{ include('thepage/_delete_form.html.twig') }}
{% endblock %}
