{% extends 'template.html.twig' %}

{% block title %}L'article{% endblock %}

{% block body %}
    <h1>L'article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>L'id</th>
                <td>{{ thepage.idthepage }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ thepage.thetitle }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ thepage.thelongtext }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ thepage.thetime ? thepage.thetime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('thepage_edit', {'idthepage': thepage.idthepage}) }}">Editer</a>
    <br><br>
    <a href="{{ path('thepage_index') }}">Retour à la liste</a>
<br><br>

    {{ include('thepage/_delete_form.html.twig') }}
{% endblock %}
